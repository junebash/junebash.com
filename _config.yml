title: "Jon Bash -- composer, sound designer, etc"
sass:
  load_paths:
    - _sass
    - node_modules
exclude: []
assets: /assets
images: /assets/images
defaults:
  -
    values:
      author: Jon Bash
      layout: default

